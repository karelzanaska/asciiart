package asciiart.image.importers

import asciiart.image.models.image.RGBImage
import org.scalatest.FunSuite

class FileSystemImageImporterTest extends FunSuite {

//  test("importImage should return RGBImage when given file path is valid") {
//    val filePath = "src/test/resources/sun.png"
//    val importer = FileSystemImageImporter(filePath)
//    val result = importer.importImage()
//    assert(result.isRight)
//    assert(result.right.get.isInstanceOf[RGBImage])
//  }
//
//  test("importImage should return error when given file path is not valid") {
//    val filePath = "src/test/resources/invalid.jpg"
//    val importer = FileSystemImageImporter(filePath)
//    val result = importer.importImage()
//    assert(result.isLeft)
//    assert(result.left.get == "Unable to load image with provided path: '" + filePath + "'")
//  }
//
//  test("importImage should return error when given file path is not image") {
//    val filePath = "src/test/resources/note.txt"
//    val importer = FileSystemImageImporter(filePath)
//    val result = importer.importImage()
//    assert(result.isLeft)
//    assert(result.left.get == "Unable to load image with provided path: '" + filePath + "'")
//  }

}
